---
title: Control Flow
description: Conditions, loops, switch, and loop control keywords.
---

# Control Flow

## If / Else

```js
prakritiIf (score > 80) {
  priyoTell("Great")
} prakritiElse {
  priyoTell("Keep going")
}
```

## While

```js
priyoKeep i = 0
prakritiAsLongAs (i < 3) {
  priyoTell(i)
  i = i + 1
}
```

## For

```js
prakritiCount (priyoChange i = 0; i < 5; i = i + 1) {
  priyoTell(i)
}
```

## Foreach

```js
priyoKeep items = ["A", "B", "C"]
prakritiCount (item priyoInside items) {
  priyoTell(item)
}
```

## Switch

```js
prakritiChoose (role) {
  prakritiCase ("admin") { priyoTell("Full access") }
  prakritiOtherwise { priyoTell("Limited access") }
}
```

## Break / Continue

- `prakritiStop` breaks loop or switch
- `prakritiGoOn` continues current loop

<Callout title="Title" type="idea">
  Use `switch` when you compare one value against many constant cases.
</Callout>
