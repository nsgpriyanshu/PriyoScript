---
title: Arrays
description: First-class arrays, indexing, slicing, destructuring, and helper APIs.
---

# Arrays

## Literals and Indexing

```js
priyoKeep nums = [10, 20, 30]
priyoTell(nums[0])
nums[1] = 25
```

## Slicing

```js
priyoTell(nums[0:2])
priyoTell(nums[:2])
priyoTell(nums[1:])
```

## Destructuring

```js
priyoChange[(first, (second = 99))] = nums
```

## Helpers

`priyoArray` provides helpers like:

- `length`, `push`, `pop`
- `slice`, `includes`, `indexOf`
- `map`, `filter`, `reduce`, `find`, `some`, `every`

## Complete example

```js
monalisa {
  lisaaTask isEven(x) {
    priyoGiveBack x % 2 == 0
  }

  priyoKeep values = [1, 2, 3, 4, 5]
  priyoKeep evens = priyoArray.filter(values, isEven)
  priyoTell(priyoArray.length(evens))
}
```

<Callout title="Title" type="idea">
  Use `priyoArray.*` helpers for cleaner code when transforming collections.
</Callout>
